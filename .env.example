# Porta do servidor (Railway define automaticamente)
PORT=3000

# Ambiente de execução
NODE_ENV=production

# CORS - URL do frontend em produção (será definida automaticamente pelo Railway)
# Deixe em branco para usar o domínio fornecido pelo Railway
CLIENT_URL=
